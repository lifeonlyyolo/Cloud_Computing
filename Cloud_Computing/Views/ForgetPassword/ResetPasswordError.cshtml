<style>
    .tm-video-wrapper {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        overflow: hidden;
        background-color: black;
    }


    #tm-video {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        min-width: 100%;
        min-height: 100%;
        width: auto;
        height: auto;
        object-fit: cover;
    }


    .login-container, .tm-container, .full-screen-center {
        position: relative;
        z-index: 10;
    }
</style>

<div class="login-container">
    <div class="login-card text-center" style="text-align: center;">
        <div class="error-icon" style="margin-bottom: 20px;">
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="#dc3545" stroke-width="2">
                <circle cx="12" cy="12" r="10"></circle>
                <line x1="12" y1="8" x2="12" y2="12"></line>
                <line x1="12" y1="16" x2="12.01" y2="16"></line>
            </svg>
        </div>
        <h2 class="login-title">Invalid Link</h2>
        <p class="login-subtitle">The reset link is invalid or has expired. Please request a new one.</p>

        <div style="margin-top: 30px;">
            <a href="/ForgetPassword/Index" class="login-button" style="text-decoration: none; display: block; background: #6c757d;">
                Try Again
            </a>
        </div>
    </div>
    <div class="tm-video-wrapper">
        <video autoplay muted loop id="tm-video">
            <source src="~/video/wave-cafe-video-bg.mp4" type="video/mp4">
        </video>
    </div>
</div>

<script src="~/js/jquery-3.4.1.min.js"></script>
<script>
    function setVideoSize() {
        const vidWidth = 1920;
        const vidHeight = 1080;
        const windowWidth = window.innerWidth;
        const windowHeight = window.innerHeight;

        const tempVidWidth = windowHeight * vidWidth / vidHeight;
        const tempVidHeight = windowWidth * vidHeight / vidWidth;

        const newVidWidth = tempVidWidth > windowWidth ? tempVidWidth : windowWidth;
        const newVidHeight = tempVidHeight > windowHeight ? tempVidHeight : windowHeight;

        $('#tm-video').css({
            'width': newVidWidth,
            'height': newVidHeight
        });
    }

    $(document).ready(function () {
        setVideoSize();
        // 監聽視窗縮放
        window.onresize = function () {
            setTimeout(setVideoSize, 100);
        };
    });
</script>